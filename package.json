{
  "name": "minify-css-selectors",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "description": "PostCSS plugin to minify css selectors in .css files and .js files for SolidJs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lMINERl",
  "peerDependencies": {
    "postcss": "^8.4.4"
  },
  "keywords": [
    "postcss",
    "minify-css-selectors",
    "minify",
    "css",
    "javascript",
    "selectors",
    "postcss-plugin",
    "css"
  ],
  "license": "MIT",
  "engines": {
    "node": ">= 10"
  },
  "dependencies": {},
  "devDependencies": {
    "postcss": "^8.4.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lMINERl/minify-css-selectors.git"
  }
}
